# WhitelistBypass Wrapper: Архитектура и дорожная карта проекта

## 1. Введение и концепция

Проект решает задачу обеспечения стабильного выхода в интернет в условиях блокировок по «белым спискам» (когда операторы связи блокируют доступ ко всем внешним ресурсам, оставляя доступными только сервисы из белого списка: Яндекс, VK, Госуслуги и др.).

### Принцип работы
Сетевой L4-трафик (TCP/UDP) перехватывается на клиентском устройстве (Android), маскируется под полезную нагрузку WebRTC (видеопоток кодека VP8 для Яндекс.Телемост или WebRTC DataChannels для VK Звонков) и передается через официальные публичные медиа-серверы (SFU). На выходной ноде (VPS) headless-демон Creator распаковывает полезную нагрузку и отправляет сетевые запросы в свободную сеть (напрямую в РФ или через upstream SOCKS5 прокси).

---

## 2. Архитектурные правила и принципы

1. **Строгое правило 1:1:1 (1 Пользователь = 1 Аккаунт = 1 Активная комната):**
   - Платформы видеоконференций не терпят многопользовательских коллизий внутри одной WebRTC-сессии.
   - У каждого пользователя на сервере выделен свой файл cookies (`cookies.json`) и отдельный изолированный инстанс демона-создателя.
2. **Полупостоянные ссылки (Ротация по требованию):**
   - Ссылка на комнату сохраняется в файле `room.env` (`CALL_LINK=...`) и не меняется при обычных перезапусках демона.
   - Новая чистая комната создается только по явной команде пользователя (`rotate`), когда старая сессия деградировала или завершилась.
3. **Надежная генерация ссылок (Native `--write-file`):**
   - Никаких эвристических ожиданий (`sleep 3`) или убийств процессов (`SIGTERM`).
   - Бинарник ядра при запуске сам записывает актуальный URL в файл `--write-file current_link`, откуда ссылка надежно считывается оркестратором.
4. **Защита от ловушки Android Private DNS (DoT):**
   - Android по умолчанию отправляет запросы DNS-over-TLS на порт 853 оператора связи (например, МТС `213.87.x.x:853`). В условиях белых списков эти запросы падают по таймауту, из-за чего ОС считает интернет отсутствующим.
   - Требование к клиентам: Private DNS должен быть выключен в системе, а в приложении Joiner жестко задан публичный DNS (`77.88.8.8` или `8.8.8.8`).

---

## 3. Стек технологий и компонентов

- **Ядро туннелирования:** скомпилированные бинарники из [kulikov0/whitelist-bypass](https://github.com/kulikov0/whitelist-bypass):
  - `headless-telemost-creator` (Яндекс.Телемост, VP8 video)
  - `headless-vk-creator` (VK Звонки, SCTP DataChannels)
  - `headless-wbstream-creator`, `headless-dion-creator` (опционально)
- **Супервизор процессов:** systemd (`whitelist-bypass@<username>.service`).
- **Системный оркестратор:** Bash-утилита `/usr/local/bin/whitelist-bypass`.
- **Диспетчеры управления (Фаза 2):**
  - VK-бот (VK LongPoll API) — доступен пользователю со смартфона даже при полностью закрытом внешнем интернете.
  - Telegram-бот (aiogram / python-telegram-bot) — для пользователей с доступным Telegram или альтернативными каналами.

---

## 4. Иерархия файлов и директорий

### На сервере (Production)

```text
/etc/whitelist-bypass/
└── users/
    ├── danya/
    │   ├── user.conf       # [0600] PROVIDER=telemost, UPSTREAM_SOCKS=...
    │   ├── cookies.json    # [0600] Сессионные куки пользователя
    │   ├── room.env        # [0600] CALL_LINK="https://telemost.yandex.ru/j/..."
    │   └── current_link    # [0644] Файл текущей активной ссылки
    └── friend/
        ├── user.conf       # PROVIDER=vk
        └── cookies.json

/opt/whitelist-bypass/
├── bin/
│   ├── headless-telemost-creator   # [0755] Бинарники ядра
│   ├── headless-vk-creator         # [0755]
│   └── bot.py                      # [0755] Диспетчер (Фаза 2)
└── venv/                           # Виртуальное окружение Python (uv)

/usr/local/bin/
└── whitelist-bypass                # [0755] Главная CLI-утилита оркестрации

/etc/systemd/system/
└── whitelist-bypass@.service       # Шаблон сервиса для пользователей
```

---

## 5. Дорожная карта разработки

### Этап 1. Системный слой оркестрации (Завершено)
- [x] Разработка универсального CLI-оркестратора `bin/whitelist-bypass`.
- [x] Реализация команд: `run`, `get-link`, `rotate`, `list`, `start`, `stop`, `restart`, `status`.
- [x] Поддержка провайдеров: `telemost`, `vk`, `wbstream`, `dion`.
- [x] Шаблон службы systemd `whitelist-bypass@.service` с изоляцией.
- [x] Настройка прав sudoers `sudoers/whitelist-bypass`.
- [x] Документация по ручному развертыванию (`docs/SETUP.md`) и настройке Android (`docs/CLIENT_ANDROID.md`).

### Этап 2. Слой интерфейсов и ботов (В процессе)
- [ ] Разработка общего Python-клиента ядра (`wlb_core.py`) для вызова команд оркестратора и чтения состояния пользователей.
- [ ] VK Бот:
  - Авторизация по `user_id` через конфигурационный файл `config.json`.
  - Команды `/link`, `/rotate`, `/status`, `/stop`, `/help`.
  - Уведомления об аварийных остановках службы.
- [ ] Telegram Бот:
  - Авторизация по `telegram_user_id`.
  - Интерактивное меню с Inline-кнопками для получения ссылки и быстрой ротации.

### Этап 3. Мониторинг и самовосстановление
- [ ] Команда расширенного аудита `whitelist-bypass healthcheck <username>` (проверка валидности cookies, доступности SFU).
- [ ] Периодический фоновый монитор зависших комнат.

### Этап 4. Инсталлятор
- [ ] Bash-скрипт установки `install.sh` для быстрого развертывания на чистый VPS.
